services:
  jenkins:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost:8080/step-rpc/v1/"]
      interval: 5s
      timeout: 5s
      retries: 40
      start_period: 60s
